[build]
  base = "apps/web"
  command = "npm install --legacy-peer-deps && npx astro build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PUBLIC_API_URL = "http://144.202.117.160:3201"
