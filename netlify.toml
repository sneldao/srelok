[build]
  base = "apps/web"
  command = "npm install --legacy-peer-deps && npx astro build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PUBLIC_API_URL = "https://api.srelok.trustfall.xyz"
