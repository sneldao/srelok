---
order: 4
label: Build
for: Jurors
stamp: MetaEvidence first
hint: MetaEvidence
detail: Schema cross-check, CAIP-10 addresses, required columns only.
aside: MetaEvidence is the schema jurors read if someone challenges. item.json must match it column for column — no extra fields, no invented labels.
---

Assemble a registry-specific item.json from the seed-first pattern. Jurors should be able to open the payload and see the same columns the registry promised.
